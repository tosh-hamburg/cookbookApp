<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp"
    android:background="?attr/selectableItemBackground">

    <!-- Meal Type Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/iconMealType"
            android:layout_width="16dp"
            android:layout_height="16dp"
            app:tint="@color/text_secondary"
            tools:src="@drawable/ic_breakfast" />

        <TextView
            android:id="@+id/textMealType"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:textSize="12sp"
            android:textColor="@color/text_secondary"
            tools:text="Frühstück" />

    </LinearLayout>

    <!-- Recipe Content (when recipe is selected) -->
    <LinearLayout
        android:id="@+id/recipeContentContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:background="@drawable/meal_slot_filled_background"
        android:padding="8dp"
        android:orientation="vertical"
        android:visibility="gone"
        tools:visibility="visible">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Recipe Image -->
            <com.google.android.material.card.MaterialCardView
                android:layout_width="48dp"
                android:layout_height="48dp"
                app:cardCornerRadius="8dp"
                app:cardElevation="0dp">

                <ImageView
                    android:id="@+id/imageRecipe"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    tools:src="@drawable/placeholder_recipe" />

            </com.google.android.material.card.MaterialCardView>

            <!-- Recipe Info -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/textRecipeTitle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLines="2"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:textColor="@color/text_primary"
                    tools:text="Spaghetti Carbonara" />

            </LinearLayout>

            <!-- Remove Button -->
            <ImageButton
                android:id="@+id/btnRemoveRecipe"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_close"
                app:tint="@color/text_hint"
                android:contentDescription="@string/remove_recipe" />

        </LinearLayout>

        <!-- Servings Control -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="start|center_vertical"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/btnDecreaseServings"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:background="@drawable/circle_background"
                android:src="@drawable/ic_minus"
                app:tint="@color/text_primary"
                android:contentDescription="@string/decrease_servings" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="8dp"
                android:gravity="center"
                android:orientation="horizontal">

                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:src="@drawable/ic_servings"
                    app:tint="@color/text_secondary" />

                <TextView
                    android:id="@+id/textServings"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="4dp"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:textColor="@color/text_primary"
                    tools:text="2" />

            </LinearLayout>

            <ImageButton
                android:id="@+id/btnIncreaseServings"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:background="@drawable/circle_background"
                android:src="@drawable/ic_plus"
                app:tint="@color/text_primary"
                android:contentDescription="@string/increase_servings" />

        </LinearLayout>

    </LinearLayout>

    <!-- Empty Slot (when no recipe) -->
    <LinearLayout
        android:id="@+id/emptySlotContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:background="@drawable/meal_slot_empty_background"
        android:gravity="center"
        android:orientation="horizontal"
        android:padding="16dp"
        tools:visibility="gone">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_add"
            app:tint="@color/text_hint" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="@string/add_recipe"
            android:textSize="14sp"
            android:textColor="@color/text_hint" />

    </LinearLayout>

</LinearLayout>
